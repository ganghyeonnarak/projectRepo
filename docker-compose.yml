services:
  my-mysql:
    image: mysql:latest
    container_name: mysql-container
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: YES
      MYSQL_DATABASE: my_erd_db
    ports:
      - "4000:3306"
    volumes:
      - mysql_data_new:/var/lib/mysql

volumes:
  mysql_data_new:
